services:
  mongo:
    image: mongo:latest
    container_name: DSW-UTN-Mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - dsw-utn-mongo-data:/data/db
    networks:
      - dsw-utn-net

  postgres:
    image: postgres:latest
    container_name: DSW-UTN-Postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: dsw_123!
    ports:
      - "5432:5432"
    volumes:
      - dsw-utn-postgres-data:/var/lib/postgresql/data
    networks:
      - dsw-utn-net

  mysql:
    image: mysql:8.4
    container_name: DSW-UTN-MySQL
    restart: unless-stopped
    command: --mysql-native-password=ON
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - dsw-utn-mysql-data:/var/lib/mysql
    networks:
      - dsw-utn-net

networks:
  dsw-utn-net:
    name: dsw-utn-net

volumes:
  dsw-utn-mongo-data:
  dsw-utn-postgres-data:
  dsw-utn-mysql-data:
